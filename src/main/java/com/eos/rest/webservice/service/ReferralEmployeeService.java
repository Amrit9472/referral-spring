package com.eos.rest.webservice.service;

import com.eos.rest.webservice.dto.ReferralEmployeeDto;
import com.eos.rest.webservice.entity.ReferralEnitity;

public interface ReferralEmployeeService {

	ReferralEmployeeDto createReferralEmployee(ReferralEnitity entity);

}
